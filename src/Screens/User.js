import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const User = () => {
  return (
    <View>
      <Text>User</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
